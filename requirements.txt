# requirements.txt — reconstitué depuis les imports du code
# (le repo a été livré sans fichier de dépendances)
fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
alembic
pydantic[email]
pydantic-settings
passlib[bcrypt]
bcrypt==4.0.1        # passlib 1.7.4 casse avec bcrypt >= 4.1/5.x ("password cannot be longer than 72 bytes") → pin
pyjwt
redis
aiohttp
requests

# --- tests (non fournis à l'origine) ---
pytest
httpx
